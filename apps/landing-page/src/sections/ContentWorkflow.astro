---
import {
   Cog,
   Lightbulb,
   Search,
   FileText,
   CheckCircle,
   Code,
} from "@lucide/astro";

const steps = [
   {
      icon: Cog,
      title: "1. Set Your Strategy Once",
      description:
         "Configure your AI agent with your brand's unique voice, audience, and purpose to create a consistent content foundation.",
   },
   {
      icon: Lightbulb,
      title: "2. Kickstart with a Simple Idea",
      description:
         "Provide your agent with a topic, a list of keywords, or approve an AI-generated idea to kickstart the creation process.",
   },
   {
      icon: Search,
      title: "3. Automated Research & Analysis",
      description:
         "The agent analyzes SERPs, competitor articles, and your internal Brand Brain to gather the most relevant information.",
   },
   {
      icon: FileText,
      title: "4. On-Brand First Drafts, Instantly",
      description:
         "A full-length, structured article is written from scratch, adhering strictly to your brand guidelines and voice.",
   },
   {
      icon: CheckCircle,
      title: "5. Review, Tweak, and Approve",
      description:
         "Review the generated draft in your dashboard, make final tweaks with the inline editor, and approve it for publishing with a single click.",
   },
   {
      icon: Code,
      title: "6. Publish with the SDK",
      description:
         "Deploy your polished content directly to your blog or any application using our powerful TypeScript SDK.",
   },
];
---

<section id="content-workflow" class="bg-muted relative py-16 md:py-24">
  <div class="mx-auto max-w-6xl px-6">
    <div class="mx-auto max-w-3xl text-center">
      <h2 class="text-balance text-2xl font-bold md:text-3xl lg:text-4xl">
        From Idea to Published Article in <span class="text-primary"
          >Minutes, Not Weeks.</span
        >
      </h2>
      <p class="text-muted-foreground mx-auto mt-4 max-w-2xl text-balance">
        ContentaGen automates the entire content pipeline while maintaining the
        quality and brand consistency you demand. Your workflow, fully
        automated.
      </p>
    </div>

    <div class="relative mt-20 max-w-3xl mx-auto">
      <!-- The vertical connecting line -->
      <div
        class="absolute left-4 top-4 h-full w-px bg-border -translate-x-1/2"
      >
      </div>

      <div class="space-y-12">
        {
          steps.map(({ icon: Icon, title, description }) => (
            <div class="relative flex items-start gap-6">
              <div class="flex-shrink-0">
                <div class="relative flex h-8 w-8 items-center justify-center rounded-full bg-primary text-primary-foreground ring-8 ring-background">
                  <Icon class="h-4 w-4" />
                </div>
              </div>
              <div class="bg-card border rounded-lg p-6 flex-1 shadow-sm hover:shadow-md transition-shadow">
                <h3 class="text-xl font-semibold">{title}</h3>
                <p class="mt-2 text-muted-foreground">{description}</p>
              </div>
            </div>
          ))
        }
      </div>
    </div>
  </div>
</section>

---
import { sdk } from "../contentagen";
import BlogPost from "../layouts/BlogPost.astro";

const { slug } = Astro.params;
const post = await sdk.getContentBySlug({ slug: slug ?? "" });
---
<BlogPost post={post} />
